/**
 * @dwhi/ui
 *
 * Reusable React Native primitives + theme tokens. Domain-agnostic by
 * contract — anything that mentions pantry/workout/household concepts
 * stays in the consuming app or its domain package.
 */
export * from './components';
export * from './theme';
