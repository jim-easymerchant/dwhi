/**
 * Design tokens shared across apps. Apps can layer additional tokens
 * on top without forking this object — keep the canonical palette,
 * radii, spacing, and typography ramp here.
 */
export { colors, radii, spacing, typography } from '@/theme/colors';
